# Direct dependencies only — transitive deps are resolved by pip automatically.
# Trimmed from 139 exact pins to fix pip "resolution-too-deep" on Termux.
#
# PennyLane (quantum computing) requires pennylane-lightning which needs
# scipy-openblas32 — unavailable on Termux/Android.  Install separately:
#   pip install --no-deps PennyLane>=0.44.1
# The app works without it (all imports are guarded with try/except).
beautifulsoup4>=4.12
duckduckgo_search>=6.0
fastapi>=0.110
httpx>=0.25
internetarchive>=3.0
nltk>=3.8
numpy>=2.0
openpyxl>=3.1
pillow>=10.0
pydantic>=2.0
pymongo>=4.5
PyPDF2>=3.0
pytesseract>=0.3.10
python-docx>=1.0
python-dotenv>=1.0
python-pptx>=0.6.21
requests>=2.31
scipy>=1.11
uvicorn>=0.25
wolframalpha>=5.0
wolframclient>=1.1
